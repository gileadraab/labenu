//Crie uma função que recebe uma string como parâmetro e retorna a mesma em caixa alta.

export const toUpperCase = (string: string): string => {
  const uppercaseString = string.toUpperCase()

  return uppercaseString
}