//Crie uma função que recebe uma string e retorna um number representando a quantidade de caracteres da mesma.

export const stringLength = (string: string): number => {
  const stringLength = string.length

  return stringLength
}